server:
  port: 8401

spring:
  application:
    name: seed-monitor-admin
  boot:
    admin:
      ui:
        title: ${spring.application.name}
  security:
    user:
      name: seed
      password: 123456